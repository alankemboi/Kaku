const startCommand = require('./start')
const settingsCommand = require('./settings')
const helpCommand = require('./help')
const donateCommand = require('./donate')
const privacyCommand = require('./privacy')

module.exports = {
  startCommand,
  settingsCommand,
  helpCommand,
  donateCommand,
  privacyCommand,
}
